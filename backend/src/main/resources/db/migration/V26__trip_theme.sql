-- 여행마다 제 색과 표식.
--
-- 목록에서 여행 넷이 나란히 서면 전부 같은 흰 줄이라, 이름을 읽어야만
-- 어느 것인지 알 수 있습니다. 매년 가는 곳이면 이름까지 비슷합니다
-- ("오사카", "오사카 2").
--
-- 색과 이모지는 이름을 읽기 전에 눈에 걸립니다. 안 정해도 됩니다 —
-- 안 정한 것은 지금까지처럼 무채색입니다. 기본값을 억지로 정해 주면
-- 정한 것과 안 정한 것을 구별할 수 없고, 정하는 일 자체가 뜻을 잃습니다.
--
-- 색은 날짜 띠가 쓰는 여덟 가지 중에서만 고릅니다. 아무 색이나 받으면
-- 흰 글씨가 안 읽히는 색이 들어오고, 코랄과 붙는 색이 들어옵니다.

ALTER TABLE trips ADD COLUMN IF NOT EXISTS theme  varchar(7);
ALTER TABLE trips ADD COLUMN IF NOT EXISTS emoji  varchar(16);
