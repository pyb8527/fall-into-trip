-- 지도에서 나를 가리키는 표시.
--
-- 동행자 위치를 이름 첫 글자를 적은 동그라미로 그리고 있었다. 이름이 "지영"
-- 이든 "지훈" 이든 지도에는 똑같이 "지" 하나만 뜬다. 누가 어디 있는지 보라고
-- 켠 것인데 정작 누구인지가 안 보인다.
--
-- 사람마다 그림을 하나 고르게 한다. 이모지 자체가 아니라 짧은 이름(rabbit,
-- bear …)만 둔다 — 이모지는 기기마다 다르게 생기고, 나중에 그림을 바꾸고
-- 싶을 때 쌓인 값을 전부 고쳐야 한다.
ALTER TABLE users ADD COLUMN mark VARCHAR(24);
