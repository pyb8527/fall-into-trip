# 아이콘과 시작 화면

넷 다 **이서윤체로 구운 그림**입니다. 화면 안의 로고는 글자로 그리지만
(`src/ui/logo.tsx`), 앱 아이콘과 시작 화면은 그림이어야 하므로 여기 둡니다.

| 파일 | 쓰는 곳 | 바탕 | 글자 |
|---|---|---|---|
| `icon.png` | 앱 아이콘 (iOS·일반) | `#0E0E0E` | 흰색 |
| `android-icon-foreground.png` | 안드로이드 적응형 아이콘 앞면 | 투명 | 흰색 |
| `splash-icon.png` | 시작 화면 | 투명 | `#0E0E0E` |
| `favicon.png` | 브라우저 탭 | `#0E0E0E` | 흰색 |

바탕색은 `app.json` 이 정합니다 — 안드로이드 적응형은 `adaptiveIcon.backgroundColor`,
시작 화면은 `expo-splash-screen` 의 `backgroundColor`.

## 다시 구우려면

```
python assets/images/bake-icons.py
```

## 왜 선을 한 겹 두르나

손글씨는 획이 가늡니다. 그냥 찍으면 64px 파비콘에서 획이 사라집니다.
채우기 둘레에 같은 색 선을 둘러 두께를 조금 보탭니다 — **글꼴 파일은
안 건드리고 그림만 두껍게 그리는 것**입니다. 흥국생명 라이선스가 금하는 것은
글꼴 파일 자체의 수정·재배포이고, 그 글꼴로 그린 그림은 자유롭게 씁니다
(로고·CI 까지 허용됩니다).

크기마다 선 굵기가 다릅니다. 1024 에서는 3%, 64 에서는 4.5% — 작을수록
더 보태야 획이 남습니다.

## 안드로이드는 동그랗게 잘립니다

적응형 아이콘은 가운데 66% 밖이 잘려 나갈 수 있습니다. 그래서 앞면 그림만
글자를 작게(칸의 46%) 잡아 두었습니다. 나머지는 잘릴 일이 없어 60~66% 입니다.
