/**
 * 디자인 토큰.
 *
 * <p>값을 눈대중으로 고르지 않았습니다. 색과 글자 크기는 토스 디자인
 * 시스템(TDS)이 공개한 값을 그대로 옮겼고, 누르는 크기는 애플·구글·WCAG 의
 * 기준을 따랐습니다. 근거는 docs/design.md 에 정리해 두었습니다.
 *
 * <p>밝은 화면 한 벌만 씁니다. 두 벌을 두면 어느 한쪽은 늘 덜 손질된 채로
 * 남고, 색을 하나 고칠 때마다 두 군데를 맞춰야 합니다.
 *
 * <p>화면 코드에서는 숫자를 직접 적지 않고 여기 이름만 씁니다.
 */

import '@/global.css';

import { Platform } from 'react-native';

/* ------------------------------------------------------------------- 색 */

/**
 * 검정과 흰색, 그리고 그 사이.
 *
 * <p>강조색을 따로 두지 않습니다. <b>가장 진한 것이 곧 강조</b>입니다.
 * 색으로 끌던 눈길을 이제 굵기와 여백이 대신합니다.
 *
 * <p>회색은 어느 쪽으로도 기울이지 않았습니다. 지도의 색(날짜 구분)만은
 * 그대로 두기로 했는데, 바탕이 조금이라도 기울면 그 색들이 다르게
 * 보입니다.
 *
 * <p>층은 그림자가 아니라 <b>선 한 가닥</b>이 나눕니다. 그림자는 카드가
 * 여럿 놓이면 화면 전체가 부옇게 뜨고, 무엇을 먼저 봐야 할지 흐립니다.
 */
const Ink = {
  /** 카드와 판이 놓이는 흰 종이 */
  0: '#FFFFFF',
  /** 흰 것 위에 다시 한 겹 — 판 안의 입력칸 */
  50: '#F9FAFB',
  /**
   * 바닥.
   *
   * <p>흰색이 아닙니다. 한동안 바닥도 흰색, 카드도 흰색이었고 그 둘을
   * 머리카락 굵기 선 하나가 갈랐습니다. 그러면 화면이 <b>테두리 쳐진
   * 사각형의 더미</b>가 됩니다 — 무엇이 무엇 위에 놓였는지는 선이 아니라
   * 밝기가 말해야 합니다.
   *
   * <p>#FAFAFA 로 한 단만 내려 봤더니 흰 카드와 차이가 1.03 배라 눈에 거의
   * 안 걸렸습니다. 여기까지 내려야 카드가 종이로 읽힙니다(1.10 배).
   */
  100: '#F2F4F6',
  /** 눌렸을 때, 그리고 선 */
  200: '#E5E8EB',
  /** 진한 선 */
  300: '#D1D6DB',
  /** 못 누르는 글자 */
  400: '#B0B8C1',
  /** 곁다리 글자 */
  500: '#8B95A1',
  /** 보조 설명 */
  700: '#4E5968',
  /** 본문 */
  900: '#191F28',
} as const;

/**
 * 코랄 — 이 앱이 쓰는 단 하나의 색.
 *
 * <h3>글씨를 지지 않습니다</h3>
 *
 * <p>한동안 주 단추를 코랄로 채우고 흰 글씨를 얹었습니다. 그러려면 색이
 * 충분히 어두워야 하는데(AA 4.5:1), 어두운 코랄은 이미 코랄이 아니라
 * 팥색입니다. 밝게 두면 글씨가 안 읽히고 어둡게 두면 색이 죽는, 빠져나갈
 * 데가 없는 자리였습니다.
 *
 * <p>토스가 그 자리를 어떻게 지나가는지 보면 답이 있습니다 — <b>단추는 흰
 * 바탕에 검은 글씨</b>이고, 색은 숫자와 링크 <b>글자에만</b> 얹힙니다.
 * 색이 글씨를 지지 않으면 대비 문제가 통째로 사라집니다.
 *
 * <h3>그래서 셋</h3>
 *
 * <p>옅은 물은 강조하는 단추의 바탕에, 진한 쪽은 그 위와 흰 카드 위의
 * 글씨에, 가운데 것은 글씨가 얹히지 않는 자리(핀·체크·표식)에.
 */
const Coral = {
  /** 강조하는 단추의 바탕. 아주 연합니다 — 색이 덩어리가 되면 안 됩니다. */
  soft: '#FFF1F2',
  softPressed: '#FFE4E6',
  /** 글씨가 안 얹히는 자리. 지도 핀, 체크 표시, 띠. */
  base: '#E5484D',
  /** 글씨와 아이콘. 흰 카드에서 5.5:1, 회색 바닥에서 5.0:1. */
  ink: '#C2313C',
} as const;

export const Colors = {
  /* 글자 */
  text: Ink[900],
  textSecondary: Ink[700],
  textMuted: Ink[500],
  textDisabled: Ink[400],

  /*
    바탕은 종이 한 장이 아니라 두 장입니다.

    바닥이 회색이고 그 위에 흰 카드가 놓입니다. 카드에 테두리를 두르지
    않아도 어디까지가 한 덩어리인지 보입니다.
  */
  background: Ink[100],
  /** 지도 뒤에 깔리는 바닥. 지도가 뜨기 전에 잠깐 보입니다. */
  abyss: Ink[200],
  surface: Ink[0],
  /** 흰 카드 위에 다시 얹히는 것(카드 안의 칸·타일) */
  surfaceRaised: Ink[50],
  fill: Ink[100],
  fillPressed: Ink[200],
  border: Ink[200],
  borderStrong: Ink[300],
  divider: Ink[200],

  /*
    강조 — 코랄.

    <p>색을 가득 칠하는 자리는 화면에 없습니다. 강조하는 단추도 옅은 물에
    코랄 글씨이고, 그것이 회색 바닥 위 흰 단추들 사이에서 충분히 눈에
    걸립니다.
   */
  /** 글씨가 안 얹히는 자리 — 지도 핀, 체크 표시. */
  accent: Coral.base,
  accentPressed: Coral.ink,
  /** 강조하는 단추와 배지의 바탕. */
  accentSoft: Coral.soft,
  accentSoftPressed: Coral.softPressed,
  /** 코랄 바탕 위에 얹는 글씨. 흰 글씨가 아닙니다. */
  accentText: Coral.ink,
  /** 강조를 글자·아이콘으로 쓸 때. */
  accentInk: Coral.ink,
  /**
   * 코랄을 가득 칠한 자리 위에 얹는 글자.
   *
   * <p>바닥에 고정된 주 단추 하나에만 씁니다. 진한 코랄과 흰 글씨는
   * 5.5:1 이라 AA 를 넘습니다 — 밝은 코랄이었다면 3.9:1 로 못 넘습니다.
   */
  onAccent: Ink[0],

  /* 지금·오늘·여기 */
  hot: Coral.ink,
  hotSoft: Coral.soft,

  /*
    알림.

    위험한 것은 색이 아니라 <b>모양</b>으로 가릅니다 — 되돌릴 수 없는 단추만
    테두리를 두르고, 그 앞에는 늘 확인 판이 섭니다. 코랄을 여기 쓰면 강조와
    경고가 같은 색이 되어, 어느 쪽인지 글을 읽어야 압니다.
   */
  danger: Ink[900],
  dangerSoft: Ink[0],
  dangerSoftPressed: Ink[100],
  success: Ink[900],
  successSoft: Ink[50],
  warning: Ink[700],
  warningSoft: Ink[50],

  /** 색으로 채운 자리 위에 얹는 글자. 날짜 색은 모두 진합니다. */
  onDay: '#FFFFFF',
} as const;

/**
 * 날짜를 구분하는 색.
 *
 * <p>뜻이 있는 색(성공·경고)이 아니라 이름표입니다. 날짜 수만큼 돌려 쓰고,
 * 지도의 핀·동선과 목록의 날짜 표시가 같은 색을 씁니다. 색만으로 구분하지
 * 않도록 핀에는 번호와 그림도 함께 넣습니다.
 *
 * <p>여섯 가지 모두 흰 글자를 얹어도 읽힐 만큼 진합니다. 옅은 색을 섞으면
 * 그 날의 핀만 번호가 안 보입니다.
 *
 * <p>붉은 자리는 비워 두었습니다. 강조색이 코랄이라, 날짜에까지 붉은색을
 * 쓰면 그 날만 "지금 눌러야 할 것" 처럼 보입니다.
 *
 * <p><b>서버가 쓰는 것과 같은 여덟 색입니다</b>({@code DayLabels.COLORS}).
 * 날짜를 만들 때 색을 정하는 것은 서버이고 화면은 그것을 받아 그립니다.
 * 여기 있는 것은 색이 안 실려 왔을 때 쓰는 자리인데, 전에는 여섯 개짜리
 * 다른 팔레트였습니다. 그래서 화면과 서버가 같은 날을 다른 색으로 그렸습니다.
 */
export const DayColors = [
  '#3182F6',
  '#E8590C',
  '#0CA678',
  '#9C36B5',
  '#0B7285',
  '#8B5A2B',
  '#5C7F1A',
  '#7B2D4E',
] as const;

export const dayColor = (index: number) => DayColors[index % DayColors.length];

/**
 * 떠 있는 것 — 이제 뜨지 않습니다.
 *
 * <p>흰 카드가 옅은 그림자로 떠 있던 것을 걷었습니다. 카드가 서넛만
 * 놓여도 화면 전체가 부옇게 뜨고, 그 상태에서는 무엇을 먼저 봐야 할지
 * 눈이 고르지 못합니다.
 *
 * <p>층은 선 한 가닥과 여백이 나눕니다. 이름은 남겨 둡니다 — 쓰는 자리가
 * 여럿이라 지우면 그만큼 고칠 데가 늘고, 언젠가 한 곳에서만 다시
 * 띄우고 싶을 때 여기만 고치면 됩니다.
 */
export const Lift = {
  shadowColor: '#000000',
  shadowOpacity: 0,
  shadowRadius: 0,
  shadowOffset: { width: 0, height: 0 },
  elevation: 0,
} as const;

/**
 * 움직임.
 *
 * <p>길이를 화면마다 눈대중으로 고르면 어떤 것은 굼뜨고 어떤 것은 홱
 * 지나갑니다. 세 가지만 둡니다.
 *
 * <ul>
 *   <li><b>tap</b> — 눌렀을 때 손끝에 바로 붙어야 합니다. 이보다 길면
 *       눌렀는지 아닌지 헷갈립니다.</li>
 *   <li><b>base</b> — 나타나고 사라지는 것. 사람이 "움직였다" 고 느끼는
 *       가장 짧은 길이입니다.</li>
 *   <li><b>sheet</b> — 판이 오르내리는 것처럼 화면의 큰 덩어리가 자리를
 *       옮길 때.</li>
 * </ul>
 */
/**
 * 아래 갈래 띠가 먹는 높이. <b>안전영역은 안 셉니다.</b>
 *
 * <p>띠 자신과, 띠 때문에 아래를 비워야 하는 화면들이 같은 값을 봐야
 * 합니다. 두 군데에 따로 적어 두었더니 하나는 60, 하나는 68 이라 어떤
 * 화면은 띠 뒤로 한 줄이 들어가고 어떤 화면은 쓸데없이 떠 있었습니다.
 *
 * <p>재서 쓰는 편이 정확하지만, 그러려면 처음 한 번은 띠 없이 그렸다가
 * 잰 뒤 다시 그려야 해서 화면이 덜컥합니다. 띠 높이는 내용에 따라 변하지
 * 않으므로(그림 하나와 작은 글자 하나) 값으로 둡니다.
 */
export const TabDock = 62;

export const Motion = {
  tap: 110,
  base: 220,
  sheet: 320,
  /** 판이 손을 떠났을 때 붙는 느낌. 튕기지 않을 만큼만 무겁게. */
  spring: { damping: 22, stiffness: 220, mass: 0.9 },
} as const;

export type ThemeColor = keyof typeof Colors;
export type Theme = { readonly [K in ThemeColor]: string };

/* ---------------------------------------------------------------- 글자 */

/**
 * 글꼴.
 *
 * <p>웹은 프리텐다드를 우리 서버에서 함께 내보냅니다(global.css). 기기마다
 * 다른 글꼴로 그려지면 같은 화면이 아니게 되는데, 특히 안드로이드와 윈도우
 * 사이에서 한글이 눈에 띄게 다릅니다.
 *
 * <p>앱에서는 기기 것을 그대로 씁니다. 프리텐다드를 앱에 넣으려면 굵기별로
 * 파일을 등록해야 하고 그만큼 앱이 무거워집니다. iOS 는 애초에 프리텐다드가
 * 본뜬 글꼴(Apple SD Gothic Neo)을 쓰므로 차이가 거의 없습니다.
 */
export const Fonts = Platform.select({
  ios: { sans: 'LeeSeoyun', mono: 'ui-monospace' },
  default: { sans: 'LeeSeoyun', mono: 'monospace' },
  web: { sans: 'var(--font-sans)', mono: 'var(--font-mono)' },
})!;

/**
 * 모든 글자에 붙는 글꼴 이름.
 *
 * <p>웹에서만 붙입니다. 앱에서 'normal' 같은 이름을 글꼴로 넘기면 안드로이드
 * 에서는 그런 글꼴을 찾다가 글자가 아예 안 그려질 수 있습니다. 비워 두면
 * 기기 기본값을 씁니다.
 *
 * <p>아래 Type 이 이것을 함께 담고 있어, 화면들은 <code>...Type.body</code>
 * 하나만 펼치면 크기·줄높이·글꼴이 한꺼번에 붙습니다. 전에는 Fonts 를
 * 만들어 두고 어디서도 쓰지 않아, 글꼴을 실어도 화면에는 붙지 않았습니다.
 */
/*
  이제 앱에도 붙입니다.

  <p>전에는 웹에만 붙였습니다. 앱은 기기 글꼴을 쓰기로 했었고, 등록되지 않은
  이름을 넘기면 안드로이드가 글자를 아예 안 그릴 수 있어서였습니다.

  <p>지금은 글꼴을 번들에 싣고(assets/fonts) 첫 화면 전에 받아 둡니다
  (ui/hand). 받아지기 전에는 이 이름이 비어 있으므로 기기 글꼴로 그려집니다.
*/
const family = { fontFamily: Fonts.sans };

/**
 * 글자 크기와 줄 높이 — TDS 본 타이포그래피 7단계 그대로입니다.
 *
 * 큰 글자일수록 줄 높이 비율이 낮습니다(30/40 = 1.33, 13/19.5 = 1.5).
 * 제목은 붙어 있어야 덩어리로 읽히고, 본문은 벌어져 있어야 눈이 다음 줄을
 * 찾습니다.
 *
 * 자간은 한글에서 살짝 좁혀야 성기지 않습니다. 큰 글자일수록 더 좁힙니다.
 */
export const Type = {
  /*
    색을 걷어 냈으므로 위계를 글자가 혼자 집니다. 단계 사이가 가까우면
    무채색 화면에서는 아무 단계도 없는 것처럼 보입니다.

    <h3>자간을 조이지 않습니다</h3>

    <p>여기 숫자들은 프리텐다드에 맞춰 음수로 조여 두었던 것입니다. 반듯한
    고딕은 글자 폭이 고르게 짜여 있어 조여야 덩어리로 읽히는데, 손글씨는
    글자마다 폭이 들쭉날쭉해서 조이면 <b>획이 서로 닿습니다.</b>

    <p>굵기도 이제 위계를 안 집니다(Weight 참고). 크기와 색이 집니다.
  */
  display: { ...family, fontSize: 34, lineHeight: 40, letterSpacing: 0 },
  title: { ...family, fontSize: 26, lineHeight: 33, letterSpacing: 0 },
  heading: { ...family, fontSize: 21, lineHeight: 28, letterSpacing: 0 },
  subheading: { ...family, fontSize: 18, lineHeight: 26, letterSpacing: 0 },
  /** 본문·입력칸. 16 아래로 내리면 iOS 사파리가 입력할 때 화면을 확대합니다. */
  body: { ...family, fontSize: 16, lineHeight: 25, letterSpacing: 0 },
  bodySmall: { ...family, fontSize: 14, lineHeight: 21, letterSpacing: 0 },
  caption: { ...family, fontSize: 12, lineHeight: 18, letterSpacing: 0 },
  /**
   * 구역 이름표.
   *
   * <p>제목을 하나 더 늘리는 대신 이것을 씁니다. 아주 작고 넓게 벌린
   * 대문자는 읽으라고 있는 것이 아니라 <b>여기서부터 다른 이야기</b>
   * 라는 표시라, 화면에 글자가 늘어도 눈이 걸리지 않습니다.
   */
  label: { ...family, fontSize: 11, lineHeight: 16, letterSpacing: 1.4 },
} as const;

/**
 * 글자 굵기.
 *
 * <h3>전부 400 입니다</h3>
 *
 * <p>이서윤체는 <b>1종</b>입니다 — 굵기 파일이 하나뿐입니다. 없는 굵기를
 * 적어 두면 브라우저와 기기가 <b>합성</b>합니다. 획을 조금 옆으로 밀어 한 번
 * 더 그리는 방식인데, 반듯한 고딕에서는 그럭저럭 봐줄 만해도 손글씨처럼 획이
 * 가늘고 굽은 글꼴에서는 획끼리 붙어 <b>먹으로 번진 것처럼</b> 됩니다.
 * 실제로 첫 판에서 제목이 그렇게 나왔습니다.
 *
 * <h3>그러면 위계는 무엇이 지는가</h3>
 *
 * <p>여태 굵기가 졌습니다("무채색에서 가장 강한 것을 만들 방법은 굵기뿐").
 * 이제 <b>크기와 색</b>이 집니다 — 제목은 더 크고, 곁다리는 더 옅습니다.
 * 그래서 Type 의 단계 사이를 벌려 두었습니다.
 *
 * <p>이름은 남겨 둡니다. 화면 이백 군데가 이 이름들을 부르고 있어서, 지우면
 * 그 자리마다 "무엇으로 바꿀지" 를 다시 정해야 합니다. 나중에 굵기가 여럿인
 * 글꼴로 갈아탈 일이 생기면 여기 숫자만 되돌리면 됩니다.
 */
export const Weight = {
  regular: '400',
  medium: '400',
  semibold: '400',
  bold: '400',
  heavy: '400',
} as const;

/* ---------------------------------------------------------------- 치수 */

/** 4의 배수로만 씁니다. 사이사이 값을 끼워 넣으면 금세 어긋납니다. */
export const Spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  huge: 40,
} as const;

/**
 * 모서리.
 *
 * <h3>각진 것을 걷었습니다</h3>
 *
 * <p>전에는 전부 직각이었습니다. 둥근 것이 여럿 겹치면 화면이 물러 보인다는
 * 이유였고, 그 말 자체는 맞습니다. 그런데 직각에 머리카락 굵기 회색 선과
 * 무채색까지 겹치니 <b>스타일을 안 입힌 것</b>으로 읽혔습니다. 각진 것이
 * 문제가 아니라, 각진 것을 받쳐 줄 면과 깊이가 하나도 없던 것이 문제였습니다.
 *
 * <p>이제 면이 층을 이루고(회색 바닥, 흰 카드) 색이 하나 들어왔으므로
 * 모서리를 둥글려도 물러 보이지 않습니다. 오히려 크게 둥글려야 카드가
 * 바닥에서 떨어져 보입니다 — 선을 안 두르기로 했으니 모서리가 그 일을
 * 대신합니다.
 *
 * <h3>세 단만 둡니다</h3>
 *
 * <p>자리마다 눈대중으로 고르면 같은 층에 놓인 것들이 서로 다른 반지름을
 * 갖게 됩니다. 그러면 카드 안에 든 것이 카드보다 더 둥글어지는 일이 생기고,
 * 그것이 화면을 어설프게 만듭니다.
 */
export const Radius = {
  none: 0,
  /** 칸 안에 드는 작은 것 — 타일, 체크, 아이콘 단추. */
  sm: 10,
  /** 단추와 줄. */
  md: 14,
  /** 카드와 판. 회색 바닥 위에 놓이는 큰 덩어리입니다. */
  lg: 20,
  /** 지도 핀, 도장, 그리고 알약 모양 칩. */
  full: 999,
} as const;

/**
 * 손가락으로 누르는 크기.
 *
 * <ul>
 *   <li>애플은 44pt, 구글은 48dp 를 최소로 봅니다.</li>
 *   <li>WCAG 2.2 는 24px 를 바닥으로 두되 AAA 에서 44px 를 요구합니다.</li>
 * </ul>
 *
 * 그래서 어떤 것도 44 아래로 내려가지 않게 합니다. 줄 안에 들어가는 작은
 * 버튼은 보기에만 작게 두고 hitSlop 으로 실제 누르는 넓이를 44 로 채웁니다.
 * 애플도 "보이는 크기와 누르는 넓이는 다를 수 있다" 고 씁니다.
 */
export const Tap = {
  min: 44,
  /** 화면의 주 동작. 넉넉히 둡니다. */
  control: 52,
  /** 줄 안에 들어가는 작은 버튼의 보이는 높이. */
  compact: 36,
  /** compact 를 44 로 채우기 위한 여유. (36 + 4*2 = 44) */
  compactSlop: 4,
} as const;

/**
 * 화면 안에서 큰 덩어리(제목·카드·목록) 사이의 간격.
 *
 * 카드 안쪽 간격(Spacing.md)보다 훨씬 넓어야 "이건 다른 이야기" 로 읽힙니다.
 * 4의 배수 눈금에서는 한 칸이 없어 따로 이름을 붙여 둡니다.
 */
export const ScreenGap = 36;

/**
 * 화면 좌우 여백.
 *
 * 가장 좁은 폰(360dp)에서도 양옆 20 씩 떼면 320 이 남아 한 줄에 한글이
 * 충분히 들어갑니다.
 */
export const Gutter = 20;

/**
 * 넓은 화면에서 글줄이 지나치게 길어지지 않게 잡아 둡니다.
 *
 * 태블릿·웹에서 한 줄이 길어지면 눈이 다음 줄 첫 글자를 잃습니다.
 */
export const MaxContentWidth = 560;
