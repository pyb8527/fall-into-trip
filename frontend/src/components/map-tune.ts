/**
 * 지도가 쓰는 눈금.
 *
 * <h3>지도가 둘이라 값도 둘이 됐습니다</h3>
 *
 * <p>웹은 구글 지도 JS, 앱은 react-native-maps 입니다(trip-map.web / trip-map).
 * 같은 지도인데 SDK 가 달라 파일이 둘인데, <b>숫자까지 둘이 됐습니다.</b>
 * 묶는 간격 46 은 앱에 상수로, 웹에는 함수 안에 그냥 적혀 있었고, 담을 때
 * 두는 여백은 웹의 두 자리에 각각 리터럴로 적혀 있었습니다.
 *
 * <p>그러면 한쪽만 고치게 됩니다. 그리고 <b>아무도 안 알려 줍니다</b> — 타입도
 * 테스트도 "이 둘은 같아야 한다" 를 모릅니다. 한 화면을 폰과 브라우저에서
 * 열었을 때 다르게 보이고 나서야 압니다.
 *
 * <p>고칠 자리를 여기 하나로 둡니다.
 */

/**
 * 한 곳을 들여다볼 때.
 *
 * <p>두 값이 같은 눈금입니다. <b>단위가 달라서 하나로 못 적습니다</b> —
 * 구글 지도 JS 는 배율(zoom)로 말하고, react-native-maps 는 위도 폭으로
 * 말합니다. 둘을 정확히 환산하려면 화면 높이가 있어야 하는데 그것은 기기마다
 * 다릅니다.
 *
 * <p>그래서 억지로 계산해 두지 않고 <b>나란히</b> 둡니다. 하나를 고치면 바로
 * 옆의 것도 같이 고치라는 뜻입니다.
 */
export const FOCUS_ZOOM = 16;
/** {@link FOCUS_ZOOM} 과 같은 눈금. 같이 고칩니다. */
export const FOCUS_SPAN = 0.006;

/**
 * 여러 곳을 한 화면에 담을 때 가장자리에 두는 여유.
 *
 * <p>웹은 fitBounds 에, 앱은 fitToCoordinates 에 그대로 넘깁니다. 아래가
 * 판에 덮여 있으면 {@code bottom} 에 그만큼 더합니다.
 */
export const EDGE = { top: 48, right: 40, bottom: 40, left: 40 } as const;

/**
 * 담을 것들 둘레에 두는 여유(배).
 *
 * <p>앱에서 위도·경도 폭을 직접 셀 때 씁니다. 웹은 fitBounds 가 대신
 * 해 주므로 안 씁니다.
 */
export const PAD = 1.35;

/**
 * 별을 묶는 간격(화면 픽셀).
 *
 * <p>별 하나가 21픽셀이라 이보다 좁으면 서로 겹칩니다. 조금 넉넉하게 잡아야
 * 스쳐 지나가듯 붙은 것까지 묶입니다.
 */
export const CLUMP_PX = 46;

/**
 * 핀을 그림으로 굽는 동안 열어 두는 시간(밀리초).
 *
 * <p>앱에서만 씁니다 — 앱 지도는 화면 요소로 만든 핀을 그림 한 장으로 구워
 * 얹습니다. 글꼴이 늦게 잡히는 기기까지 여유를 두되, 이 시간 동안은 핀마다
 * 매 프레임 다시 구우므로 길게 잡으면 지도가 무거워집니다.
 */
export const DRAW_MS = 400;
