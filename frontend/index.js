/*
  앱(안드로이드·iOS)의 시작점.

  <h3>왜 갈랐나</h3>

  화면은 웹과 앱이 한 벌이었지만, 리액트 네이티브는 그 한 벌을 <b>두 엔진</b>
  으로 번역했습니다. 웹에서는 div 와 CSS 가 되고 앱에서는 안드로이드 뷰가
  됩니다. 그래서 같은 코드인데 그림자가 사방으로 뿌려지고, 지도는 제 겉면을
  따로 가져 그 위의 것이 깜빡이고, 여백 단위가 한쪽만 달랐습니다. 쪽마다
  고쳐야 하는 자리가 계속 나왔고 그때마다 앱 쪽만 확인 없이 나갔습니다.

  이제 앱은 <b>웹을 띄우기만</b> 합니다. 그리는 것이 한 벌이 되니 그 갈림이
  통째로 없어집니다. 폰만 할 수 있는 일(로그인 창, 알림, 인쇄, 위치)은
  껍데기가 맡아 웹에 건네줍니다.

  <p>src/app 아래 화면들은 <b>지우지 않았습니다.</b> 웹이 그대로 쓰고 있고,
  애플 심사나 다른 데서 막히면 돌아올 자리가 있어야 합니다.
*/
import { registerRootComponent } from 'expo';

import { Shell } from '@/shell/shell';

registerRootComponent(Shell);
